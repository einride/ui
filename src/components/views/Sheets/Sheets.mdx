import {
  Meta,
  Primary,
  Description,
  Source,
  Title,
  ArgTypes,
  Subtitle,
  Controls,
  Story,
  Canvas,
  Stories,
  Markdown,
} from "@storybook/blocks"
import * as Sheets from "./Sheets.stories.tsx"

<Meta of={Sheets} />
<Title />
<Description of={Sheets} />

```tsx
import { Sheets } from "@einride/ui"
```

## Basic

<Description of={Sheets.Basic} />
<Canvas
  of={Sheets.Basic}
  source={{
    code: `
const { isOpen, handlers } = useDisclosure(true)
return (
  <>
    <PrimaryButton onClick={handlers.open}>Open sheets</PrimaryButton>
    <Sheets
      closeHandler={handlers.close}
      isOpen={isOpen}
      navigationAction={{
        "aria-label": "Close",
        icon: "xMark",
        onClick: handlers.close,
      }}
      navigationTitle="Page name"
      primaryAction={{ children: "Primary" }}
    >
      <Text>Sheets content</Text>
    </Sheets>
  </>
)
`,
  }}
/>
<Controls of={Sheets.Basic} />
