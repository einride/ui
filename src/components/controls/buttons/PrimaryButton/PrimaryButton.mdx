import {
  Meta,
  Primary,
  Description,
  Source,
  Title,
  ArgTypes,
  Subtitle,
  Controls,
  Story,
  Canvas,
  Stories,
  Markdown,
} from "@storybook/blocks"
import * as PrimaryButton from "./PrimaryButton.stories.tsx"

<Meta of={PrimaryButton} />
<Title />
<Description of={PrimaryButton} />

```tsx
import { PrimaryButton } from "@einride/ui"
```

## Basic

<Description of={PrimaryButton.Basic} />
<Canvas of={PrimaryButton.Basic} />
<Controls of={PrimaryButton.Basic} />

## Full width

<Description of={PrimaryButton.FullWidth} />
<Canvas of={PrimaryButton.FullWidth} />
<Controls of={PrimaryButton.FullWidth} include="isFullWidth" />

## With icon

<Description of={PrimaryButton.IconRight} />
<Canvas of={PrimaryButton.IconRight} />
<Controls of={PrimaryButton.IconRight} include="rightIcon" />

## Loading

<Description of={PrimaryButton.IsLoading} />
<Canvas of={PrimaryButton.IsLoading} />
<Controls of={PrimaryButton.IsLoading} include="isLoading" />
