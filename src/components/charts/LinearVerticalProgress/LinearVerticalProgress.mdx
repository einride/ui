import {
  Meta,
  Primary,
  Description,
  Source,
  Title,
  ArgTypes,
  Subtitle,
  Controls,
  Story,
  Canvas,
  Stories,
  Markdown,
} from "@storybook/blocks"
import * as LinearVerticalProgress from "./LinearVerticalProgress.stories.tsx"

<Meta of={LinearVerticalProgress} />
<Title />
<Description of={LinearVerticalProgress} />

```tsx
import { LinearVerticalProgress } from "@einride/ui"
```

## Basic

<Description of={LinearVerticalProgress.Basic} />
<Canvas of={LinearVerticalProgress.Basic} />
<Controls of={LinearVerticalProgress.Basic} />

## Custom range

<Description of={LinearVerticalProgress.CustomRange} />
<Canvas of={LinearVerticalProgress.CustomRange} />
<Controls of={LinearVerticalProgress.CustomRange} include={["min", "max", "value"]} />

## Range color

<Description of={LinearVerticalProgress.CustomColor} />
<Canvas of={LinearVerticalProgress.CustomColor} />
<Controls of={LinearVerticalProgress.CustomColor} include="color" />

## Accessibility

Adding a descriptive `aria-label` is required to convey what the progress bar is displaynig.
