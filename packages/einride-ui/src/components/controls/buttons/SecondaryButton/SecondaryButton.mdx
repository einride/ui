import {
  Meta,
  Primary,
  Description,
  Source,
  Title,
  ArgTypes,
  Subtitle,
  Controls,
  Story,
  Canvas,
  Stories,
  Markdown,
} from "@storybook/blocks"
import * as SecondaryButton from "./SecondaryButton.stories.tsx"

<Meta of={SecondaryButton} />
<Title />
<Description of={SecondaryButton} />

```tsx
import { SecondaryButton } from "@einride/ui"
```

## Basic

<Description of={SecondaryButton.Basic} />
<Canvas of={SecondaryButton.Basic} />
<Controls of={SecondaryButton.Basic} />

## Full width

<Description of={SecondaryButton.FullWidth} />
<Canvas of={SecondaryButton.FullWidth} />
<Controls of={SecondaryButton.FullWidth} include="isFullWidth" />

## With icon

<Description of={SecondaryButton.IconRight} />
<Canvas of={SecondaryButton.IconRight} />
<Controls of={SecondaryButton.IconRight} include="rightIcon" />

## Loading

<Description of={SecondaryButton.IsLoading} />
<Canvas of={SecondaryButton.IsLoading} />
<Controls of={SecondaryButton.IsLoading} include="isLoading" />
