import {
  Meta,
  Primary,
  Description,
  Source,
  Title,
  ArgTypes,
  Subtitle,
  Controls,
  Story,
  Canvas,
  Stories,
  Markdown,
} from "@storybook/blocks"
import * as TextInput from "./TextInput.stories.tsx"

<Meta of={TextInput} />

<Title />

<Description of={TextInput} />

```tsx
import { TextInput } from "@einride/ui"
```

## Basic

<Description of={TextInput.Basic} />

<Canvas of={TextInput.Basic} />

<Controls of={TextInput.Basic} />

## Without label

<Description of={TextInput.WithoutLabel} />

<Canvas of={TextInput.WithoutLabel} />

<Controls of={TextInput.WithoutLabel} include={["aria-label", "placeholder"]} />

## Message

<Description of={TextInput.Message} />

<Canvas of={TextInput.Message} />

<Controls of={TextInput.Message} include={["message"]} />

## Status message

<Description of={TextInput.Message} />

<Canvas of={TextInput.Message} />

<Controls of={TextInput.Message} include={["message", "status"]} />

## Suffix

<Description of={TextInput.Suffix} />

<Canvas of={TextInput.Suffix} />

<Controls of={TextInput.Suffix} include={["suffix"]} />

## Accessibility

A descriptive `label` or `aria-label` is required to make sure the input has an accessible name.

Prefer a visible label when possible.
