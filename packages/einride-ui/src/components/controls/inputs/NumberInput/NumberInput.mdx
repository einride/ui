import {
  Meta,
  Primary,
  Description,
  Source,
  Title,
  ArgTypes,
  Subtitle,
  Controls,
  Story,
  Canvas,
  Stories,
  Markdown,
} from "@storybook/blocks"
import * as NumberInput from "./NumberInput.stories.tsx"

<Meta of={NumberInput} />

<Title />

<Description of={NumberInput} />

```tsx
import { NumberInput } from "@einride/ui"
```

## Basic

<Description of={NumberInput.Basic} />

<Canvas of={NumberInput.Basic} />

<Controls of={NumberInput.Basic} />

## Without label

<Description of={NumberInput.WithoutLabel} />

<Canvas of={NumberInput.WithoutLabel} />

<Controls of={NumberInput.WithoutLabel} include={["aria-label", "placeholder"]} />

## Message

<Description of={NumberInput.Message} />

<Canvas of={NumberInput.Message} />

<Controls of={NumberInput.Message} include={["message"]} />

## Status message

<Description of={NumberInput.Message} />

<Canvas of={NumberInput.Message} />

<Controls of={NumberInput.Message} include={["message", "status"]} />

## Suffix

<Description of={NumberInput.Suffix} />

<Canvas of={NumberInput.Suffix} />

<Controls of={NumberInput.Suffix} include={["suffix"]} />

## Accessibility

A descriptive `label` or `aria-label` is required to make sure the input has an accessible name.

Prefer a visible label when possible.
